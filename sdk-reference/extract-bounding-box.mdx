---
title: 'Extract bounding box'
description: 'Find the bounding box of an element given a natural language description.'
---

## extract_bbox
```python
def extract_bbox(
    self,
    element_description: str,
    state: PIL.Image | None = None,
    annotate: bool = True
) -> List[int]:
```

<ParamField path="element_description" type="string" required>
  Natural language description of the element to extract.
</ParamField>

<ParamField path="state" type="Image.Image">
  Screenshot of the current screen. If not provided, a new screenshot of the browser will be taken.
</ParamField>

<ParamField path="annotate" type="boolean" default="True">
  Whether to annotate the element on the browser.
</ParamField>

<ResponseField name="return" type="List[int, int, int, int]">
  List of 4 integers representing the bounding box of the element.
  The order is [x1, y1, x2, y2] where (x1, y1) is the top-left corner and (x2, y2) is the bottom-right corner.
</ResponseField>

